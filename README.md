# Ruwiki Explain

Веб‑приложение, которое берёт статью по теме и делает понятное объяснение для детей 6–14 лет: главная мысль, простой текст, персональная аналогия, термины, примеры, мини‑викторина и метрики качества.

## Возможности

- **Возрастные группы**: 6–8, 9–11, 12–14 лет
- **3 режима**: очень просто / сбалансированно / подробнее
- **Персонализация**: категории интересов и короткий контекст о ребёнке
- **Кэш ответов**: быстрый exact-cache в SQLite и похожие запросы через Chroma
- **Метрики**: достоверность к источнику, ROUGE‑1, ROUGE‑L, BLEURT‑proxy
- **Озвучка** и копирование результата
- **Живой UX**: прогресс, подсказки ожидания и эффект печатающей машинки

## 🏗️ Архитектура

```
├── frontend/           # Статический сайт (HTML/CSS/JS)
│   ├── index.html     # Главная страница
│   ├── styles.css     # Стили
│   └── app.js         # Логика фронтенда
├── backend/           # FastAPI приложение
│   ├── app/          # Основной код
│   ├── requirements.txt
│   └── .env          # Конфигурация
└── .sourcecraft/       # CI/CD (опционально)
```

## 🔧 Локальная разработка

### Backend (FastAPI)
```bash
cd backend
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или .venv\Scripts\activate  # Windows
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```
Откройте `http://127.0.0.1:8000/docs` для доступа к API документации.

### Frontend
```bash
cd frontend
python -m http.server 5173
```
Откройте `http://127.0.0.1:5173`. По умолчанию frontend обращается к `http://127.0.0.1:8000`.

## Настройка

Создайте `backend/.env` (можно начать с `backend/.env.example`) и укажите провайдера модели и ключи, если нужны.

## 📊 API

- `POST /simplify` — упрощает статью по теме, возрасту и режиму.
- `GET /health` — показывает провайдера модели, имя модели и наличие API-ключа.
- `DELETE /cache` — очищает SQLite-кэш и историю генераций.

Пример запроса:
```json
{
  "query": "Квантовая механика",
  "age": 10,
  "mode": "balanced",
  "interest_topics": ["Космос и наука"],
  "child_notes": "любит роботов"
}
```
`mode` принимает значения `simple`, `balanced` или `detailed`.

## 🧠 Интеграция с LLM

`backend/app/services/llm.py` содержит единый интерфейс для моделей:
- `openai_compatible` — любой OpenAI‑совместимый endpoint
- `gemini` — Gemini API
- `ollama` — локальный Ollama

Бизнес-логика в `pipeline.py` не зависит от конкретного провайдера.

## 🔄 Пайплайн обработки

1. Получает статью Рувики через MediaWiki API
2. Нормализует входной текст и выбирает фрагмент для объяснения
3. Проверяет быстрый кэш похожих вопросов по эмбеддингу запроса и возрастной группе
4. Проверяет exact-cache с учетом темы, возрастной группы, режима, статьи и версии промпта
5. Запрашивает у модели строгий JSON с тезисом, объяснением, шагами, терминами, аналогиями и вопросами
6. Проверяет, не был ли ответ обрезан или заблокирован провайдером
7. Прогоняет результат через локальный readability guard
8. Считает достоверность к источнику, ROUGE‑1, ROUGE‑L и BLEURT‑proxy
9. При необходимости запускает repair-cycle
10. Сохраняет прошедший проверку результат в SQLite и Chroma для будущих быстрых ответов

## 📝 Лицензия

MIT License. Используются статьи из Рувики (CC BY-SA 3.0).