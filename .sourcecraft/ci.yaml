on:
  push:
    - workflows: [test-workflow]
      filter:
        branches: [main]

workflows:
  test-workflow:
    tasks:
      - name: test-task
        cubes:
          - name: hello-cube
            script:
              - echo "Hello, world!"